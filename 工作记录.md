# 工作记录：基于文本的视频异常行为检测

这份文件记录每次工作的实际进展。每次结束时，在下方追加日期、做过的事、确认的结果和下一步；旧记录保留。文中“已完成”只表示已经实际检查过。

## 2026-09-24：确定方向和准备环境

- 确定研究目标：给定视频和一段事件描述，找出对应的时间段，并为每帧或每段给出分数。
- 形成修订版实施规划，放在 doc 文件夹。后续工作以该规划为准。
- 建立项目目录和 Git 记录。原始数据、模型和运行结果放在本地，不纳入 Git。
- 下载 AnyAnomaly 官方代码，固定到提交 ec7e9fc36e56ca16a5fa08386e368a7e4d18dbb0。
- 建立独立的 Python 3.10.21 环境。确认本机显卡是 RTX 4060 Laptop，显存约 8 GB。
- 目前机器没有 WSL。后续需要 vLLM 的正式大模型实验，计划在 Linux GPU 环境进行；本机先做数据准备和小规模验证。

## 2026-09-25：第一步，取得并核对 Avenue 数据

已完成：

- 从 CUHK 官方数据集页面下载 Avenue_Dataset.zip；文件大小 813,227,845 字节。压缩包逐项校验通过，SHA-256 为 FC9CB8432A11CA79C18AA180C72524011411B69D3B0FF27C8816E41C0DE61531。
- 解压到 datasets/avenue/raw/Avenue Dataset。原始数据包含 16 个训练视频、21 个测试视频，以及每个视频对应的 MATLAB 文件。
- 在独立环境安装 OpenCV 4.10.0.84 和 h5py 3.16.0。21 个测试视频都能读取；总计 15,324 帧，实际读出的帧数与视频记录一致，帧率均为 25 帧/秒。
- 对照 AnyAnomaly 仓库的 C-Ave 标签：常规五类事件（靠得太近、骑自行车、投掷、奔跑、跳舞）覆盖 18 个测试视频，共 12,394 帧；这 18 个视频的标签长度与各自帧数全部一致。
- 其余 06、09、11 号视频属于复合事件，仓库另有单独标签。后续做常规五类事件评估时，不能把这 3 个视频混进来。
- 安装 PyTorch 2.6.0（CUDA 12.6）并确认它能调用 RTX 4060。安装 CLIP 1.0.1，使用第 16 号视频的第 101 帧和两句示例文字成功完成一次 ViT-B/32 推理；计算约 0.57 秒，显卡内存峰值约 354 MiB。两个示例相似度为 0.2390、0.2834，仅用于证明流程能运行，不代表检测准确率。
- 首次导入 CLIP 时发现环境里的 setuptools 83 已移除 pkg_resources，而 CLIP 仍依赖它。把本项目独立环境固定到 setuptools 80.9.0 后，导入和推理均成功。Windows 本地测试所用依赖版本保存在 requirements-windows.txt。
- 将本次数据核对与工作记录提交到 Git，检查点为 f841d2f；原始数据、模型权重和独立环境都没有进入 Git。

下一步：

- 把官方视频整理成参考代码需要的 JPG 帧目录，并再次核对帧数、排序及 24 帧分段的尾部处理。
- 在整理好的少量 JPG 帧上核对 CLIP 输入与 AnyAnomaly 参考代码的处理方式，然后再开始完整评估流程。

数据来源：CUHK Avenue 官方页面 https://www.cse.cuhk.edu.hk/leojia/projects/detectabnormal/dataset.html
参考代码：https://github.com/SkiddieAhn/Paper-AnyAnomaly
依赖兼容说明：https://github.com/pypa/setuptools/blob/main/docs/deprecated/pkg_resources.rst

## 2026-09-25：补充安装位置

- 本项目的 Python 解释器在 E:\Video_Anomaly_Detection\codex\.conda\local\python.exe。PyTorch、CLIP、OpenCV、h5py 等已安装的 Python 包在该环境的 Lib\site-packages 文件夹。
- Avenue 压缩包及解压后的原始视频在 E:\Video_Anomaly_Detection\codex\datasets\avenue\raw。CLIP ViT-B/32 模型权重在 E:\Video_Anomaly_Detection\codex\models\clip\ViT-B-32.pt。AnyAnomaly 参考代码在 E:\Video_Anomaly_Detection\codex\third_party\Paper-AnyAnomaly。
- pip 的下载缓存位置是 C:\Users\56364\AppData\Local\pip\cache。这是安装包缓存，不是项目运行环境。电脑原有的 Conda 主程序位于 D:\anaconda3；Conda 也可能使用 D:\anaconda3\pkgs 或用户目录下的缓存。
- 上述项目数据、模型、独立环境和参考代码没有提交到 Git；仓库只保存实施规划、工作记录、说明和依赖版本清单。
