# 工作记录：基于文本的视频异常行为检测

这份文件记录每次工作的实际进展。每次结束时，在下方追加日期、做过的事、确认的结果和下一步；旧记录保留。文中“已完成”只表示已经实际检查过。

## 2026-09-24：确定方向和准备环境

- 确定研究目标：给定视频和一段事件描述，找出对应的时间段，并为每帧或每段给出分数。
- 形成修订版实施规划，放在 doc 文件夹。后续工作以该规划为准。
- 建立项目目录和 Git 记录。原始数据、模型和运行结果放在本地，不纳入 Git。
- 下载 AnyAnomaly 官方代码，固定到提交 ec7e9fc36e56ca16a5fa08386e368a7e4d18dbb0。
- 建立独立的 Python 3.10.21 环境。确认本机显卡是 RTX 4060 Laptop，显存约 8 GB。
- 目前机器没有 WSL。后续需要 vLLM 的正式大模型实验，计划在 Linux GPU 环境进行；本机先做数据准备和小规模验证。

## 2026-09-25：第一步，取得并核对 Avenue 数据

已完成：

- 从 CUHK 官方数据集页面下载 Avenue_Dataset.zip；文件大小 813,227,845 字节。压缩包逐项校验通过，SHA-256 为 FC9CB8432A11CA79C18AA180C72524011411B69D3B0FF27C8816E41C0DE61531。
- 解压到 datasets/avenue/raw/Avenue Dataset。原始数据包含 16 个训练视频、21 个测试视频，以及每个视频对应的 MATLAB 文件。
- 在独立环境安装 OpenCV 4.10.0.84 和 h5py 3.16.0。21 个测试视频都能读取；总计 15,324 帧，实际读出的帧数与视频记录一致，帧率均为 25 帧/秒。
- 对照 AnyAnomaly 仓库的 C-Ave 标签：常规五类事件（靠得太近、骑自行车、投掷、奔跑、跳舞）覆盖 18 个测试视频，共 12,394 帧；这 18 个视频的标签长度与各自帧数全部一致。
- 其余 06、09、11 号视频属于复合事件，仓库另有单独标签。后续做常规五类事件评估时，不能把这 3 个视频混进来。
- 安装 PyTorch 2.6.0（CUDA 12.6）并确认它能调用 RTX 4060。安装 CLIP 1.0.1，使用第 16 号视频的第 101 帧和两句示例文字成功完成一次 ViT-B/32 推理；计算约 0.57 秒，显卡内存峰值约 354 MiB。两个示例相似度为 0.2390、0.2834，仅用于证明流程能运行，不代表检测准确率。
- 首次导入 CLIP 时发现环境里的 setuptools 83 已移除 pkg_resources，而 CLIP 仍依赖它。把本项目独立环境固定到 setuptools 80.9.0 后，导入和推理均成功。Windows 本地测试所用依赖版本保存在 requirements-windows.txt。
- 将本次数据核对与工作记录提交到 Git，检查点为 f841d2f；原始数据、模型权重和独立环境都没有进入 Git。

下一步：

- 把官方视频整理成参考代码需要的 JPG 帧目录，并再次核对帧数、排序及 24 帧分段的尾部处理。
- 在整理好的少量 JPG 帧上核对 CLIP 输入与 AnyAnomaly 参考代码的处理方式，然后再开始完整评估流程。

数据来源：CUHK Avenue 官方页面 https://www.cse.cuhk.edu.hk/leojia/projects/detectabnormal/dataset.html
参考代码：https://github.com/SkiddieAhn/Paper-AnyAnomaly
依赖兼容说明：https://github.com/pypa/setuptools/blob/main/docs/deprecated/pkg_resources.rst

## 2026-09-25：补充安装位置

- 本项目的 Python 解释器在 E:\Video_Anomaly_Detection\codex\.conda\local\python.exe。PyTorch、CLIP、OpenCV、h5py 等已安装的 Python 包在该环境的 Lib\site-packages 文件夹。
- Avenue 压缩包及解压后的原始视频在 E:\Video_Anomaly_Detection\codex\datasets\avenue\raw。CLIP ViT-B/32 模型权重在 E:\Video_Anomaly_Detection\codex\models\clip\ViT-B-32.pt。AnyAnomaly 参考代码在 E:\Video_Anomaly_Detection\codex\third_party\Paper-AnyAnomaly。
- pip 的下载缓存位置是 C:\Users\56364\AppData\Local\pip\cache。这是安装包缓存，不是项目运行环境。电脑原有的 Conda 主程序位于 D:\anaconda3；Conda 也可能使用 D:\anaconda3\pkgs 或用户目录下的缓存。
- 上述项目数据、模型、独立环境和参考代码没有提交到 Git；仓库只保存实施规划、工作记录、说明和依赖版本清单。

## 2026-09-25：确认以后下载软件的规则

- 以后每次准备下载或安装软件、Python 包等软件依赖时，先向用户说明名称、计划安装的完整路径，以及可能用到的下载缓存路径；等用户确认路径后再开始。若计划路径改变，重新确认。
- 这次只记录规则，没有下载或安装新软件。

## 2026-09-25：第二步，准备视频帧并核对标签

已完成：

- 编写 scripts/prepare_avenue_frames.py，并先用小型测试视频验证帧顺序、重复运行和错误标签的处理；3 项测试通过。脚本没有修改原始 AVI。
- 将 21 个 Avenue 测试视频转为 datasets/avenue/testing/frames/01 到 21 的 JPG 目录，共 15,324 张图，约 1.06 GiB。每个视频从 000001.jpg 开始连续编号。
- 用 AnyAnomaly 的帧读取器复核全部 21 个目录：读取数量、数字顺序和每 24 帧的完整片段数均与转换记录一致；常规事件的 18 个目录与标签文件中的视频键一致，标签长度也全部相符。
- 常规事件的 18 个视频共有 12,394 帧，可形成 507 个完整的 24 帧片段；各视频末尾合计还有 226 帧（约 1.82%）。参考代码按完整片段推理，并把标签截到预测长度，因此这些尾帧不进入其原始评估。以后报告指标时需要明确说明这个口径。
- 用第 16 号视频的一张 JPG 与原 AVI 对应帧进行 CLIP 输入核对：输入形状均为 1×3×224×224，图像特征相似度为 0.994399。这只是预处理检查，不是异常检测准确率。
- 再次运行转换脚本，21 个目录都显示为已核对，未重复转换。详细机器核对结果保存在 outputs/avenue_frame_audit.json，该目录不提交 Git。

发现与下一步：

- 参考仓库的 utils.py 在导入时需要 matplotlib；本项目环境目前未安装它。这不影响这次帧转换。下一阶段若需安装软件包，我会先向你确认具体安装路径和缓存路径。
- 下一阶段先核对基线运行所需的其余依赖，再用一个视频和一种事件文本试跑完整评分链路。

## 2026-09-25：连接公开 GitHub 仓库

- 确认本机已保存的 GitHub 凭据可连接到账号 c37958640-hash，无需另装软件或重新输入密码。
- 按用户要求创建公开仓库 https://github.com/c37958640-hash/text-guided-video-anomaly-detection，并把本地项目连接为 origin。
- 将截至 57cd9e0 的本地 main 分支提交推送到 GitHub，设置 main 跟踪 origin/main。
- 今后每次工作结束，先更新这份记录，再检查改动、按需本地提交并推送到 GitHub，最后核对本地与远程提交一致。大型数据、模型权重和本地 Python 环境继续留在本地。

## 2026-09-25：第三步，核对基线依赖并做单视频检查

已确认：

- 核对 AnyAnomaly 的正式脚本：它需要 fastprogress、transformers、vLLM、qwen-vl-utils 等；其评估代码还会用到 matplotlib、SciPy 和 scikit-learn。当前本地独立环境均未安装这些包。本次没有下载或安装新软件。
- vLLM 官方安装文档面向 Linux；当前 Windows 环境无法直接照搬参考仓库的 Qwen/vLLM 完整评分流程。后续正式大模型评估需要 Linux GPU 环境，并单独确认模型与软件的下载位置。
- 使用已安装的 CLIP ViT-B/32，对 Avenue 第 16 号测试视频（740 帧）和文本“a person riding a bicycle”做了本地单视频检查。方法是逐帧计算图文相似度，每 24 帧取最高值作为该片段分数，再复制到这 24 帧。30 个完整片段共评估 720 帧；按参考代码的口径，末尾 20 帧没有进入指标。
- 与参考仓库的骑车标签核对：该视频共有 99 帧标为正例；720 个纳入评估的帧中有 89 个正例，末尾排除的 20 帧中有 10 个正例。对这一个视频计算出的帧级 AUROC 为 0.998941，另行读取结果与标签复算一致。结果保存在本地 outputs/clip_bicycle_smoke.json，不提交 Git。
- 这个数值只说明这一组事先选定的视频和文字能产生可区分的分数。它没有使用 Qwen、没有覆盖全部视频和事件，也不是 AnyAnomaly 论文结果或正式基线成绩。
- 已向用户询问：matplotlib、SciPy、scikit-learn、fastprogress 计划安装在 E:\Video_Anomaly_Detection\codex\.conda\local\Lib\site-packages，下载缓存及临时文件计划放在项目内 .cache\pip 和 .cache\tmp。得到路径确认后才能安装。

下一步：

- 用户确认安装及缓存路径后，补齐 Windows 本地评估依赖，检查包冲突，并保存准确版本。
- 整理可重复运行的单视频检查，再准备在 Linux GPU 上运行 Qwen/vLLM 正式评分；正式指标需覆盖规定的事件和视频，并写清尾帧处理口径。

参考：vLLM 官方安装文档 https://docs.vllm.ai/en/latest/getting_started/installation/gpu/

## 2026-09-25：第四步，安装 Windows 本地评估依赖

已完成：

- 用户确认路径后，在项目独立环境的 .conda\local\Lib\site-packages 安装 matplotlib 3.10.9、SciPy 1.15.3、scikit-learn 1.7.2 和 fastprogress 1.0.3；这些库必需的 10 个辅助包也装在同一环境。下载缓存使用 .cache\pip，安装临时文件使用 .cache\tmp；它们都不提交到 Git。
- 起初检查到新版 fastprogress 1.1.6 会额外引入与本课题评估无关的网页服务软件包，于是在安装发生前停止，并改用能满足参考代码所需进度条功能的 1.0.3 版。
- 安装后运行依赖检查，显示“没有损坏的依赖关系”；四个库均可导入，参考仓库的 utils 和评估模块也可导入。原有的 3 项视频帧准备测试全部通过。
- 使用新装的 scikit-learn 重新计算上一阶段单视频检查结果，帧级 AUROC 仍为 0.998941。Windows 环境的准确软件版本已更新到 requirements-windows.txt。
- 本次没有下载 Qwen 模型，也没有安装 transformers、vLLM 或 qwen-vl-utils；完整 Qwen/vLLM 评分仍需 Linux GPU 环境。

下一步：

- 把单视频 CLIP 检查整理为可重复运行的脚本，明确文本、分段方式、输出和尾帧处理。
- 正式 Qwen/vLLM 评估开始前，先确认 Linux 环境以及软件、模型各自的下载位置。

fastprogress 版本参考：https://pypi.org/project/fastprogress/1.0.3/

## 2026-09-25：第五步，把单视频检查变成可重复运行的脚本

已完成：

- 新增 scripts/clip_single_video_diagnostic.py。默认读取 Avenue 第 16 号视频、文字“a person riding a bicycle”和骑车标签；也可以指定视频编号、事件文字及对应标签类别。
- 脚本用本地已有的 CLIP ViT-B/32 权重计算每帧图文相似度，再把每 24 帧的最高值作为该片段分数。JSON 报告写明每个片段的起止帧、分数、参与评估的帧数，以及末尾被排除的帧数和正例数。运行时不会下载模型。
- 对第 16 号视频重新运行：740 帧、30 个完整片段、末尾排除 20 帧（其中 10 个正例），单视频 AUROC 为 0.998941。新报告的逐帧分数、片段分数和指标与上一次保存的检查结果完全一致；输出在本地 outputs/clip_16_bicycle.json，不提交 Git。
- 补充了自动测试：核对片段分数与尾帧处理；当所选视频只有一种标签时，仍保存分数，并将无法计算的 AUROC 记为空值。连同原有测试共 5 项，全部通过。使用说明已写入 README.md。
- 这仍是单视频 CLIP 检查，不是论文的 Qwen/vLLM 基线，也不能用这个单视频 AUROC 代表整个数据集的效果。本次没有安装或下载任何软件与模型。

下一步：

- 在更多视频上按固定文字和匹配的事件标签进行检查，记录各视频差异，避免只看一个高分样例。
- 再准备 Linux GPU 上的正式 Qwen/vLLM 流程；下载任何新软件或模型前，先确认具体安装及缓存路径。

## 2026-09-25：第六步，五类事件的多视频检查

已完成：

- 在看多视频结果之前，先固定五句英文事件描述，保存到 configs/avenue_clip_prompts.json，并单独提交为 0d093c4。它们是本次探索性检查的描述，不是论文官方提示词。
- 新增 scripts/evaluate_clip_avenue.py，按参考仓库常规事件口径处理 18 个视频与五类事件，共 90 组。每张图像的 CLIP 特征只计算一次，再同时和五句描述比较；每个完整 24 帧片段取最高分。
- 每类事件共有 12,394 个原始帧、507 个完整片段、12,168 个评估帧；末尾合计 226 帧按既定口径排除。各事件把 18 个视频的评估帧合并计算 AUROC，没有对每个视频单独归一化。
- 整体 AUROC：骑车 0.992354、跳舞 0.179534、奔跑 0.857192、投掷 0.777639、靠近镜头 0.601874。骑车只在 16 号视频有正例；跳舞 17 号为 0.690192，而 18 号仅 0.120016，说明不能用单个高分视频代表整体效果。
- 将保存的片段分数重新与原始 H5 标签核对，五类 AUROC、正例帧数和尾帧数均复算一致；含正例的视频编号与参考仓库的 c-avenue.json 一致。第 16 号骑车的 AUROC 与上一轮单视频检查一致，批量计算产生的片段分数差异最大约 0.00004。
- 详细分数保存在本地 outputs/clip_avenue_5events.json；通俗易懂的结果和各正例视频成绩写在 reports/avenue_clip_5events_2026-09-25.md。原始数据、模型和详细结果不上传 GitHub。新增汇总测试后，项目 6 项自动测试全部通过。本次没有下载或安装软件与模型。

下一步：

- 查看跳舞 18 号、靠近镜头 19 号等低分视频的具体片段与分数变化，判断是描述不合适、场景差异还是 CLIP 方法本身不足；分析前不改本轮固定描述。
- 再决定下一种改进或正式 Qwen/vLLM 评估。下载新软件或模型之前，继续先向用户确认安装和缓存路径。

## 2026-09-26：解释跳舞 AUROC 偏低的原因

- 根据已经保存的结果和原始标签，进一步核对 17、18 号视频。17 号单视频 AUROC 为 0.690192，18 号仅 0.120016；18 号的跳舞帧通常比分数较高的普通帧得分更低。
- 18 号参与评估的 288 帧中，265 帧是跳舞、23 帧不是跳舞。开头第一个 24 帧片段包含 20 帧普通画面，却得到比后续纯跳舞片段更高的分数；片段分数被赋给其中全部 24 帧，使这些普通帧排在许多跳舞帧之前。
- 只合并 17、18 号视频时，AUROC 为 0.398797；加上其他 16 个不含跳舞正例的视频后降为 0.179534。这说明不同视频之间的原始 CLIP 分数尺度也影响整体排名。抽看 18 号的少量静止帧发现画面背景较多、动作不易从单帧判断，但还不能把它确定为唯一原因。
- 已将数字和通俗解释补进 reports/avenue_clip_5events_2026-09-25.md。本次没有改模型、提示词或原始结果，也没有下载软件。

下一步：

- 如需继续改进，先看 18 号视频的连续动作和分数曲线，再决定是否需要运动信息或分数校准；任何新方法另开实验，保留这一轮结果便于比较。

## 2026-09-26：继续查看跳舞失败片段

- 用现有分数和标签画出 17、18、20 号视频的时间线，并单独比较 18 号的逐帧分数与每 24 帧取最高分后的结果；图保存在本地 outputs/dancing_timeline_17_18_20.png 和 outputs/dancing_18_raw_vs_clip.png，不提交 Git。
- 18 号视频直接按逐帧分数计算的 AUROC 为 0.161772，改用 24 帧片段最高分后为 0.120016。最高的第一个片段由未标为跳舞的第 1 帧提供最高分，其中 20 帧未跳舞、4 帧跳舞。说明分段加重问题，但原始逐帧判断已经偏低。
- 20 号视频第 193–216 帧得到 0.284130 的跳舞分数，实际上 24 帧全标为投掷、没有跳舞；18 号完全属于跳舞的片段最高只有 0.241477。抽看相关连续画面，20 号人物在中间大幅移动，18 号的白衣人物靠画面左侧；这是具体错判例子，尚不足以证明唯一原因。
- 以上观察已补充到多视频检查报告。本次没有改固定文字、模型、代码或既有指标，也没有下载软件。

下一步：

- 保留本轮结果。若尝试裁剪人物区域、加入动作信息或改变分数比较方式，先写明新方法和评价口径，再作为单独实验验证，不能用这轮测试结果反复挑选设置。

## 2026-09-26：核对 AnyAnomaly 的 Qwen 运行方式，准备 Linux 阶段

- 对照本地保存的论文作者代码与官方说明，弄清了视频帧、五类标签、CLIP 权重、Qwen 模型和结果文件分别从哪里读取、写到哪里。形成了 `doc/AnyAnomaly_Qwen_Linux_运行准备.md`，README 也已指向它。
- 发现作者代码把数据根目录写成 `/home/anonymous/datasets`，默认 Qwen 模型名和 CLIP 模型名可能引发下载；在确认实际 Linux 路径前，不能直接运行默认命令。作者的 `requirements.txt` 也没有列全 Qwen 运行需要的软件。
- 发现原脚本不能单独指定第 16 号视频，而且一整类视频处理完才写出分数。下一阶段须先加入单视频入口、原始回答保存和进度保存，再用第 16 号视频的 740 帧检查是否得到 720 个有效帧分数。
- 作者默认的参数搜索会用测试标签挑选最高 AUROC。后续将先记录固定参数成绩，把这种“测试集选优”成绩另列，避免误认为它是独立测试结果。原脚本在拼接视频后做平滑，与现有 CLIP 检查的指标口径也不同。
- 本次只审阅代码和资料、编写说明；没有安装软件，没有下载模型或视频，没有改动既有 CLIP 分数。

下一步：

- 请用户明确 Linux GPU 环境及项目可放置的位置。届时列出软件安装、模型、缓存与临时目录的**实际绝对路径**，在任何下载前请用户确认。
- 路径确认后准备单视频入口，先做第 16 号视频的流程验收；通过后再评估全部五类事件。

## 2026-09-26：完成 Qwen 单视频入口的 Windows 准备

- 确定后续拟用 **Qwen2.5-VL-3B-Instruct**，与作者 Qwen/vLLM 脚本默认型号一致；目前没有下载模型，也没有运行 Qwen。
- 新增 `scripts/anyanomaly_qwen_single_video.py`。默认模式只检查一个 Avenue 视频的图片顺序和对应事件标签，不加载 Qwen。只有明确给出 `--run` 和已存在的本地模型目录，才会尝试在 Linux 上调用作者的关键帧、注意力图、时序图及 Qwen 问答函数。
- 运行模式会在每个 24 帧片段结束后保存三句原始回答与三路分数。若中断，下一次可从已保存片段之后继续；超出 0～1 的分数会报错并保留原话，避免悄悄写入错误成绩。运行时强制模型离线加载，现有 CLIP 权重也通过本地文件路径加载。
- 已在 Windows 上真正运行第 16 号视频的离线检查：740 张图片、30 个完整片段、720 帧可评分；末尾 20 帧排除，其中 10 帧标为骑车。结果存放在本地 `outputs/anyanomaly/preflight_16_bicycle.json`，不上传 Git。
- 先写并运行测试，确认新入口尚不存在时测试失败；实现后，分段统计、中断恢复、越界分数与负数拦截共 4 项通过。连同原有 6 项，本次全部 10 项测试通过；脚本语法检查也通过。README 和 Linux 运行准备说明已同步更新。
- 现有 Windows 环境没有 vLLM 与 Qwen 模型，因此**真实 Qwen 推理尚未验证**。本次没有下载、安装或改动已有 CLIP 结果。

下一步：

- 确定 Linux GPU 环境及真实目录；任何软件或模型下载之前，先让用户确认安装、模型、缓存与临时文件的路径。
- 转移已有 Avenue 图片、标签、CLIP 权重及参考代码，先在 Linux 上完成第 16 号视频的真实 Qwen 检查，再考虑五类完整评估。
